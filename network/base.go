package network

// Base implements for Module.
type Base struct{}

// Init init will be invoked when app starts
func (c Base) Init() {

}

// Shutdown shutdown will be invoked before app closes
func (c Base) Shutdown() {}
